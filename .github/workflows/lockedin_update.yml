name: Update Locked-In Playlist
on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Update playlist
        run: curl -s --max-time 120 "https://lockedin-playlist.krx18bott.workers.dev/playlist.m3u" || true
