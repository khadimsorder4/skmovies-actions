name: KV Cache Cleanup
on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: KV Cleanup
        run: curl -s --max-time 60 "https://skmovies-premium.pages.dev/api/cleanup" || true
